{
  "name": "parser",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.ts",
  "scripts": {
    "start": "cmd /c \"del dist\\bundle.js\" && gulp && cmd /c cscript dist/bundle.js",
    "regger": "cmd /c \"del dist\\bundle.js\" && gulp && cmd /c \"cscript dist/bundle.js regger\"",
    "parser": "gulp && cmd /c \"cscript dist/bundle.js parser\"",
    "build": "gulp",
    "watch": "gulp watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browserify": "^14.3.0",
    "gulp": "^3.9.1",
    "gulp-plumber": "^1.1.0",
    "gulp-watch": "^4.3.11",
    "proxy-setup": "^1.0.3",
    "tsify": "^3.0.1",
    "typescript": "^2.3.2",
    "vinyl-source-stream": "^1.1.0"
  }
}
